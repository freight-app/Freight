[package]
name        = "user32"
version     = "0"
description = "Win32 user interface — windows, menus, dialogs, message loop, input"
supports    = "windows"

[lib]
type = "system"
link = "user32"
hdrs = ["windows.h", "winuser.h"]
