[package]
name        = "winmm"
version     = "0"
description = "Windows multimedia — waveOut/waveIn audio, joystick, high-resolution timer (timeGetTime)"
supports    = "windows"

[lib]
type = "system"
link = "winmm"
hdrs = ["mmsystem.h", "timeapi.h"]
