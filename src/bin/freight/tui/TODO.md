# TUI TODO

## Done
- [x] **`freight add`** — package browser (search, select, add to `freight.toml`)
- [x] **`freight build --panel`** — live build-progress panel; auto-scroll log, spinner,
      elapsed time, success/failure summary, compiler diagnostic lines
- [x] **`freight tui`** — registry admin panel (packages, users, tokens, orgs, audit)
      with 3-pane package detail view and tui-markdown README rendering

## Planned

- [ ] **`freight outdated`** — interactive update picker; show current/latest side by side, space to toggle which to update, Enter to apply
- [ ] **`freight tree`** — navigable dependency tree; expand/collapse nodes, jump to package detail
- [ ] **`freight test`** — test runner with live output; pass/fail per test, filter by name, re-run single test on keypress
