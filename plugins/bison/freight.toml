[package]
name        = "bison"
version     = "0.1.0"
description = "Bison parser generation — compiles every .y grammar into the build."

[plugin]
entry   = "bison.freight"
handles = ["bison"]
tools   = ["bison"]
inputs  = ["**/*.y"]
