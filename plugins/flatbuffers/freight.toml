[package]
name        = "flatbuffers"
version     = "0.1.0"
description = "FlatBuffers code generation via flatc — adds [flatbuffers] to any project."

[plugin]
entry   = "flatbuffers.freight"
handles = ["flatbuffers"]
tools   = ["flatc"]
inputs  = ["**/*.fbs"]
