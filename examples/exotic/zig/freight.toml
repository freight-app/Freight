[package]
name        = "zig-hello"
version     = "0.1.0"
description = "Native Zig binary — comptime, generics, tagged unions, and error handling"

[[bin]]
name = "zig-hello"
src  = "src/main.zig"

[profile.debug]
opt-level = 0

[profile.release]
opt-level = 3
strip     = true
